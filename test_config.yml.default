###
# Security note:
# If you can insert your client id and secret
# through the MENDELEY_CLIENT_ID and MENDELEY_CLIENT_SECRET
# environment variables, please do that instead.
# Only put sensitive credentials in config files if you
# have no choice, and you don't mind accidentally
# committing them to a public Git repo one day (wahey!)
###

clientId: id
clientSecret: secret
redirectUri: http://localhost
accessToken: token
recordMode: all
